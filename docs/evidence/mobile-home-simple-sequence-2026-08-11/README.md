# Mobile Home simple sequence evidence

Production build `5qDPK8VofQ88lD8Rs32I2` verifies the direct Mobile Home correction.

- `npm run verify`: PASS, including the 31/31 current user-worktree static build.
- 390px: `scrollWidth = clientWidth = 390`; Copyright is closed by default; its summary is 44px high.
- 430px: `scrollWidth = clientWidth = 430`; Copyright is closed by default; its summary is 44px high.
- Browser order is `WORDS YOU WANNA KNOW → seven words → OVER TIME → introduction → Copyright / rights`.
- `null/` is vertical; `depression/` is to its right; `intelligence/` and `(COMING SOON)` follow directly below; `data/` closes the word composition.
- `CONTINUE`, panel breaks, `No route`, the repeated creator line, and the bottom About CTA are absent from the word field.
- `/words/forever` uses the prior public mobile renderer. No audit, gate, STOP, PASS, contract, or implementation-authorization text appears publicly. This temporary renderer is not a declaration that its legacy figures passed the new research audit.
- Desktop Home, About, `ForeverDesktopEdition`, and `ResponsiveForeverEdition` have no source diff from base HEAD `5238802`.

The current build contains the protected, untracked Privacy mobile demo and therefore reports 31 static routes. Those user-owned files were not modified or staged.
