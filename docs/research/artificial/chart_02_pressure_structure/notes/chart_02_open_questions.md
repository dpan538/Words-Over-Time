# Chart 02 Open Questions

- Should the large circle say `substitute field` or `substitute / replacement field`?
- Should the lower-left pressure circle say `not real` or `not genuine`?
- Should `artificial limb` be visible or optional?
- Should `artificial flowers` also bridge toward not-real?
- How many evidence labels can fit in the lower panel?
- Should `not natural ≠ not real` be visible by default or only on hover?
- Should `suspicion` be labeled directly on the line?
- Should `no artificial ingredients` be kept for Chart 4 instead?
- Should `all natural` stay background-only for Chart 4?
- Should `natural light ↔ artificial light` remain visible if the lower panel gets too dense?
- Should the lower panel use all three not-natural pairs, or reduce to flavor and ingredients only?
- Should the legend remain exactly `○ pressure field` / `● selected word`, or should the lower panel adapt it?
