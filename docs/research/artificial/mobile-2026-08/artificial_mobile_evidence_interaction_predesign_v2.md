# Artificial mobile — evidence-scale and synchronized narrative predesign v2

**Status:** AWAITING EXPLICIT USER APPROVAL / DO NOT IMPLEMENT THIS REVISION YET  
**Scope:** Mobile Artificial only  
**Desktop:** read-only  
**Supersedes:** the four-page composition, repeated registry summaries, and isolated-component reading order in the previous terminal rebuild  
**Does not supersede:** source lineage, Viewer production blockers, rights limits, or the requirement for typed deterministic transforms

This revision answers two connected problems: the page currently exposes small record counts without explaining the depth of the research, and its figures read as independent instruments rather than one argument. The new story therefore separates **research coverage** from **language evidence**, uses percentages only inside named curated denominators, and makes text and geometry enter the same state together.

## Principal question

Where is *artificial* already a settled functional term, and where does it remain an uncertain extension of human presence?

## Strongest supported finding

In the selected 25-record human-evidence ledger, all ten support and replacement records are coded `established_term`, while none of the five speculative-extension records are. Those five speculative-extension records comprise two `emerging_term`, two `hype_sensitive`, and one `philosophical_term` entries under the raw `speculative_status` field.

Public wording must retain **“in the selected evidence ledger.”** This is a composition of a purposively curated evidence file, not a population estimate for English, medicine, technology, or public opinion.

## Evidence-scale audit

### Active research lineage

The only large-number method figure authorized by this predesign is a branching lineage with directly labelled units:

- 335 inventoried files;
- 31 active dependencies;
- 28 active raw/source files;
- 20 active Ngram CSV files and 8 non-Ngram evidence files;
- 26,430 logical Ngram rows, excluding headers;
- 25,990 unique annual capture cells after deduplication by `term × year × corpus × smoothing × case_sensitive`;
- 440 exact duplicate rows and 0 conflicting duplicate keys;
- 9,270 frozen selected term–year cells across 42 exact terms: 6,630 cells for 30 compounds and 2,640 cells for 12 media terms;
- 53 logical non-Ngram raw records across the active evidence branch: 12 origin, 15 suspicion, and 26 human records;
- 44 records in the current frozen selection: 11 origin, 7 suspicion, and 26 human records.

This is a **branching provenance tree**, not a single conversion funnel. File counts, logical rows, annual cells, terms, and evidence records have different units. Node area, branch width, and connector length may not compare unlike units.

### Rejected large-number claims

- Do not publish `26,548 records`; that value is a physical line count containing headers and formatted JSON lines.
- Do not call 25,990 or 9,270 occurrences, books, documents, tokens, corpus size, or appearances per million.
- Do not use the archive-wide 65,175-cell audit as the current mobile sample; it contains Ngram rounds outside the active dependency closure.
- Do not publish Viewer peak chronology, cross-term frequency, adoption, or ratios. The `en` release alias remains unpinned and the selected terms mix n-gram orders.
- A Viewer value of zero remains `absent_or_suppressed`, never observed zero.

## Required new typed projections

No percentage or large-number array in this document may be hand-authored in TSX. Before page work begins, the deterministic builder and types must add:

1. `researchFootprint`
   - active file-set hash;
   - logical-row count;
   - deduplication key and duplicate/conflict counts;
   - selected term and selected term–year cell counts;
   - explicit unit and method-only eligibility.
2. `originAdjudication`
   - `core=4`, `care=4`, `method=1`, `excluded=2`, `total=11`;
   - `outsideUnreservedCore=7/11=63.6%`.
3. `suspicionExpandedPanel`
   - all 15 unique raw IDs from the four active Round 04 CSV files;
   - period, domain, source type, confidence, source metadata, and inclusion rule;
   - no inherited hand-authored `negativeCharge` for the eight newly included records.
4. `humanEvidenceProfile`
   - the context record separated from 25 evidence-bearing records;
   - confidence, current relevance, speculative status, function mode, source type, source label, and URL;
   - deterministic cross-tabs and named denominators.

Public source detail is limited to metadata, project-authored short paraphrases, and links. The repository does not carry per-source reproduction licences for copying source prose.

## Five-page 390 px storyboard

Pages 1–3 continue the supplied black terminal geometry across three authored movements. Page 4 reconstructs the white dot-matrix reference. Page 5 is a full-viewport interactive close finding. The physical phone shell is not rendered.

### Page 1 — A wide audit, a narrow claim

**Primary readout:** `25,990`  
**Unit:** unique annual capture cells audited in the active Ngram branch  
**Secondary readout:** `9,270 / 42 exact terms` retained in the frozen mobile selection

The method lineage grows from files to two independently labelled branches. The 30-compound branch becomes a deterministic five-branch circular data tree; the 12-media branch becomes four round-capped signal constellations. All record nuclei are true SVG circles. Connector length, node depth, and 3D rotation encode layout or lineage only.

Visible sentence:

> We audited **25,990 annual cells**, then retained **9,270** for a deliberately narrow 42-term register.

The two bold phrases pulse once as their corresponding branches complete growth. A compact reference-02 metric ticket below the tree states the method-only caveat and expands to the deduplication rule.

### Page 2 — Evidence is held back before it is published

**Primary readout:** `64%`  
**Unit:** 7 of 11 adjudicated origin claims not promoted to unreserved core

The reference-01 Extended panel keeps its outer proportion and segmented rhythm, but every segment is one real claim: four core, four use-with-care, one method boundary, and two excluded. No segment is repeated to imitate the reference's count.

Below it, the reference-03 paired field places the claim-state distribution beside the expanded 15-record suspicion ledger. The suspicion finding is displayed as a selected-panel contrast:

- before 1950: `0/5` records are explicit absence/removal claims;
- 1950 onward: `8/10 = 80%` are explicit `no/remove/absence` claims.

This is a purposively selected source panel, not a sentiment trend or market share. The words **held back** and **absence** enter a single soft luminance pulse when the claim segments and period branches finish growing.

A second reference-02 metric ticket exposes the four adjudication states and expands to the source/risk ledger.

### Page 3 — Function settles before presence

**Primary readout:** `100% → 0%`  
**Unit:** established-term share inside named function groups in the selected 25-record ledger

The primary geometry is a deterministic data tree/sphere hybrid with 25 circular evidence nuclei:

- support: 5/5 established terms;
- replacement: 5/5 established terms;
- continuation: 2/4 established terms;
- simulation: 5/6 established terms under `speculative_status`;
- speculative extension: 0/5 established terms.

Branch membership controls position. Circular mark treatment controls maturity. Confidence is text-redundant and may control halo intensity only after the legend names the project-internal rating. Rotation remains optional inspection; it never changes values.

Visible sentence:

> **Support** and **replacement** are settled in this ledger; **speculative presence** is not.

As each branch grows, its matching phrase receives one bounded brightness pulse. The final reference-02 metric ticket shows `19/25 = 76% high confidence`, `24 unique URLs`, and `10 source types`; expansion explains that URL uniqueness is not source independence.

The three metric tickets use the same strict card geometry but are distributed one per terminal movement, instead of forming an isolated stack of repeated cards.

### Page 4 — Evidence dot matrix

The reference-05 white field remains a 6 × 6 circular matrix with the top-right asterisk implemented as a real 44 × 44 px button. It cycles three named views over the same 25 evidence-bearing records:

1. **Maturity:** 17 established terms, 3 emerging terms, and 5 boundary-sensitive/speculative statuses;
2. **Confidence:** 19 high and 6 medium project ratings;
3. **Function:** 14 support/replacement/continuation records and 11 modeled-process records, with direct five-function labels in the detail region.

Solid, thick-ring, and thin-ring treatments always resolve through the active view's visible legend. Empty slots are labelled layout capacity, not missing or zero records. Mode changes update the large percentage, direct labels, and accessible summary together.

### Page 5 — Close finding

The final movement occupies one full viewport. It contains one small-heading-scale sentence and a large controlled evidence region:

> Artificial does not move in one line from **made** to **fake** to **intelligent**; it widens from settled **functions** into uncertain **presence**.

`made`, `fake`, `functions`, and `presence` are underlined native buttons, not links. Each button controls the same fixed evidence region:

- **made:** 4 core origin claims and the technical/making distinction;
- **fake:** the four core claims keep made/not-natural distinct from fake, while the other seven claims remain caveated, methodological, or excluded;
- **functions:** 10/10 support/replacement records coded established term;
- **presence:** 0/5 speculative-extension records coded established term, with 2 emerging and 3 speculative under current relevance.

The region has a useful default (`functions`), so the page never opens as unexplained blank space. Each state contains one number, one short interpretation, and a folded source/method disclosure. Buttons expose `aria-pressed` and `aria-controls`; the region has an accessible name and polite status update. Essential meaning is not hidden exclusively behind interaction.

## Synchronized growth contract

- A small client island observes each movement once; the full page remains server-rendered.
- No-JavaScript and reduced-motion states render the complete final geometry and text.
- The default animation is deterministic and lasts at most 1.2 seconds per movement.
- Data grows in declared tree/period/function order. There is no random scattering, idle loop, autoplay rotation, or decorative satellite particle.
- Text uses one soft opacity/luminance pulse with no rapid flash and no more than two contrast reversals. It does not exceed three flashes per second.
- A 44 px replay control may repeat the movement on explicit request; scrolling back does not retrigger it.
- 3D inspection retains vertical page scrolling, pinch zoom, keyboard rotation, reset, pointer intent threshold, and reduced-motion behavior.
- Every luminous data nucleus is circular. Lines are continuous, low-contrast membership or lineage connectors and never resemble additional data marks.

## Component and reading-order correction

The current sequence repeats small counts through a tree, bars, a sphere, three metric cards, and another matrix. The revision removes that repetition:

- one method tree establishes scale once;
- one adjudication field pairs origin and suspicion;
- one maturity geometry carries the human-process conclusion;
- the three matching metric tickets are separated across Pages 1–3;
- the dot matrix re-reads one evidence set across three categorical lenses;
- the close finding converts four conclusion words into evidence selectors.

Text is no longer telemetry placed above a finished chart. Each movement has one visible finding sentence whose emphasized phrases correspond to named mark groups and animation states. Longer interpretation, method, caveat, and sources remain inside expandable cards/disclosures.

## Reference mapping

- Reference 04 terminal pair: continuous black grid, density, telemetry scale, and circular geometric fields across Pages 1–3.
- Reference 03 linear dashboard: Page 2 paired claim/suspicion field.
- Reference 01 Extended: Page 2 adjudication panel.
- Reference 02 metric cards: one strict-proportion ticket per Pages 1–3, visually linked as a distributed set.
- Reference 05 dot matrix: Page 4 composition, 6 × 6 rhythm, circular taxonomy, and top-right switch.
- Latest terminal geometry direction: all light nuclei are circles arranged by deterministic lineage, function, period, or maturity—not random coordinates.

## Estimated rendered surface allocation

Target total at 390 px is approximately 4,528 vertical pixels:

| Surface | Approx. px | Share |
| --- | ---: | ---: |
| Primary visualization and interactive evidence fields | 2,650 | 58.5% |
| Collapsed/expandable data tickets and evidence region | 1,400 | 30.9% |
| Always-visible prose, headings, controls, and navigation | 478 | 10.6% |

This is a storyboard estimate, not a pass claim. The implementation must be measured from rendered bounding boxes at 390 px and 430 px.

## Approval gate

After explicit user approval, implementation order is:

1. extend the builder, types, contracts, source hashes, and production adapter;
2. validate all counts and reject blocked Viewer interpretations;
3. rebuild the five-page server composition;
4. add the small synchronized-growth, geometry-inspection, dot-switch, and close-finding client islands;
5. verify 390 px, 430 px, 200% reflow, touch, keyboard, reduced motion, hydration, console, surface ratio, source disclosure, and desktop non-regression.

`READY_FOR_USER_VISUAL_ACCEPTANCE=false`
