# Artificial mobile — terminal visual rebuild contract

> The four-page composition below is superseded by the pending five-page evidence/interaction predesign in `artificial_mobile_evidence_interaction_predesign_v2.md`. Do not implement the new revision until the user approves that document.

**Status:** USER-DIRECTED VISUAL REBUILD / IMPLEMENTATION AUTHORIZED  
**Scope:** Mobile Artificial only  
**Desktop:** read-only  
**Data authority:** `src/data/generated/artificial_mobile_research.json`

This addendum replaces the rejected rounded-dashboard composition. It does not replace the frozen Artificial research, findings, lineage, or figure gates. The page keeps the approved five research movements, but rebuilds their mobile visual surface from the four newly binding references.

## Reference decomposition

### A. Full-screen research terminal

- edge-to-edge near-black surface;
- a persistent one-pixel Cartesian grid rather than floating card shells;
- narrow 18–20 px working gutters at 390 px;
- several vertically partitioned evidence zones per viewport;
- white and grey quantitative marks, with Artificial wine used only for a focal state;
- compact monospace telemetry tied to actual record names, counts, periods, and source states;
- no fake device chrome, invented sensor values, or decorative data marks.

### B. Three-ticket archive

- three tall white tickets remain visible as one horizontal set;
- each ticket has a small header, a ruled geometry field, and a lower record code;
- geometry is deliberately different across the set: circular dots, square pixels, and a striped circle;
- the circular ticket maps the 30 registered exact compounds;
- the square ticket maps the 12 registered media terms;
- the striped ticket maps the 25 marked human-process evidence records;
- one dot, square, or stripe equals one registered term or source record. Ticket geometry is not frequency.

### C. Solid / hollow dot matrix

- a stark white field sits inside the black terminal surface;
- the matrix contains the 25 evidence-bearing human-process source records;
- every circle is one equal evidence record grouped by bodily support, functional replacement, reproductive continuation, or modeled process;
- layer membership, not record confidence, controls the visible grouping; the current typed artifact does not authorize a 5-high / 20-mixed confidence split;
- the single definition-family context record is labelled separately and does not become a quantitative dot;
- the matrix has a direct legend and no percentage reading.

### D. Dense terminal ledger

- the seven suspicion anchors form seven equal source rows;
- period and domain are direct text, never inferred from position alone;
- the adjudicated 0–3 negative-charge ordinal uses three cells with text redundancy;
- strength changes the mark outline and remains named in text;
- the ledger is a source-bound registry, not a population trend.

## 390 px storyboard

1. Compact site navigation on black, then `WORD STUDY`, `artificial`, and one sentence naming the research question.
2. The three-ticket archive, with all three geometries simultaneously visible.
3. Origin terminal: 11 adjudicated claims in four explicitly labelled states.
4. Compound terminal: 30 equal square marks grouped into five semantic systems.
5. Media terminal: 12 equal signal marks grouped into four registered systems.
6. Suspicion terminal: seven dense source rows with local provenance disclosures.
7. Human-process dot matrix: 25 evidence marks plus one separately labelled context record.
8. Concise closing reading, folded sources/rights, back to top, final site line.

Expected rendered surface remains visualization-led: approximately 60% primary evidence fields, 30% locally expandable record detail, and 10% navigation and always-visible prose. The final ratio is measured from rendered boxes; no fixed height, spacer, or empty padding may be used to manufacture it.

## Prohibited carry-over

- rounded dashboard-card grammar from the rejected implementation;
- black panels floating on a light article page;
- generic KPI cards;
- flip cards;
- invented telemetry, decorative barcodes, or unmatched pixels;
- current Viewer values used as shared-scale comparison;
- Desktop component, layout, narrative order, or visual geometry imports.

## Four-page reconstruction addendum — 2026-08-14

The user supplied five new binding references and explicitly approved a four-page composition. This addendum supersedes the earlier three-screen implementation while preserving every production gate above.

1. Pages 1–3 reconstruct the two terminal interiors in `codex-clipboard-49c2dcf6-233a-48c7-9fd3-5ef61c0168d5.png` across approximately three 390 px viewport lengths. The physical phone shell is not copied. The compound registry, media registry, origin claims, suspicion anchors, and human-process records receive distinct terminal geometries; blocked Viewer dates and magnitudes are absent.
2. The outlined Extended segment panel in `codex-clipboard-092495a7-6135-4aa2-a5eb-f14eba4358ee.png`, the three-panel metric stack in `codex-clipboard-ffd260e3-eeb4-468f-aed5-15d92fcdb597.png`, and the paired particle/segmented-bar plots in `codex-clipboard-c704d438-1d70-4ca2-b4c9-b1d16d92ca5c.png` are mandatory component grammars inside those first three page lengths.
3. Page 4 reconstructs the white five-column dot-matrix surface in `codex-clipboard-9d9e0e35-d2d5-419c-af31-6d7dfda9659b.png`. Its top-right asterisk is a real 44 × 44 px control that cycles named production-eligible record sets. Keyboard, touch, focus-visible, and reduced-motion behavior are required.
4. The first three pages may exceed one short viewport when necessary for readable 3D DOM/SVG fields, but their total rhythm remains three authored report movements rather than a card feed.

The five latest references are registered as `artificial-reference-09` through `artificial-reference-13` by the deterministic generator.
